const Promotor = require('../models/Promotor')

module.exports = {
  create: (req,res) => {

  },

  getAll: (req,res)=> {

  },

  getOne: (req,res) => {
    let { eventId } = req.params
    
  },

  edit: (req,res) => {
    let { eventId } = req.params

  }
}