const Promotor = require('../models/Promotor')

module.exports = {
  create: (req,res) => {

  },

  getAll: (req,res)=> {

  },

  getOne: (req,res) => {
    let { prmotorId } = req.params
    
  },

  edit: (req,res) => {
    let { promotorId } = req.params

  }
}